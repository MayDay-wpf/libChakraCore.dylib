# libChakraCore.dylib
* zh:构建好的 `libChakraCore.dylib` 适用于OSX-ARM64
* en:The well-built `libChakraCore.dylib` is suitable for OSX-ARM64

# 允许所有未知来源(建议使用后关闭)
* 终端执行 `sudo spctl --master-disable`
 ![image](https://github.com/user-attachments/assets/a0cb2a01-5fe5-49e6-bbc0-b9f4664247f5)
